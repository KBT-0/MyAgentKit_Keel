interface:
  display_name: "MyAgentKit: Claude Review"
  short_description: "Ask Claude, verify findings, and re-review fixes"
  default_prompt: "Use $myagentkit-review to ask Claude to review my changes and fix confirmed findings."
policy:
  allow_implicit_invocation: true
